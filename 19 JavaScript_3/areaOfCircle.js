/*

  5.  Write a JavaScript program that calculates the area of a circle using the Math object's properties and
    methods.

*/

const radius = 5;
const area = Math.PI * Math.pow(radius, 2);
console.log(area);